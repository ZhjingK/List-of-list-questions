比较顺序表和链表的优缺点，说说分别在什么场景下使用？
1.顺序表随机访问效率高；链表插入删除效率高；
2.顺序表需要一块连续的空间存储数据，链表的空间可以是连续的也可以是不连续的，当内存中所剩的空间不足时，顺序表的创建肯会失败；
3.顺序表使用数组下标来指示元素，链表需要用指针将节点连接起来，维护起来开销较顺序表大；
4.顺序表数据密度大，命中率高比链表高;
5.顺序表的CPU高速缓存效率更高，链表的CPU缓存效率低